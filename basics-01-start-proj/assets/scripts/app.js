let currentResult = 0;

currentResult += 10;

function add(num1, num2){
  const result = num1 + num2;
  alert(`The result is ${result}`);
}

add(1, 20);
add(20, 20);
add(30, 20);

//let calcDescription = `(${currentResult} + 10)`;

//outputResult(currentResult, calcDescription);
